# Node modules folder contains all the npm packages installed for the project
# These can be reinstalled via package.json, so we don't need to commit them
node_modules/

# Log files can get quite large and are environment-specific, so they should be ignored
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variable file contains sensitive information like API keys and database URIs
# We should never commit this to the repository
.env

# Cache and lock files are specific to the development environment and shouldn't be committed
package-lock.json
yarn.lock

# Directory for temporary files generated by the build process or while running the app
tmp/
temp/

# OS-specific files that are not relevant to the project
.DS_Store   # macOS
Thumbs.db   # Windows

# Ignore any output from IDEs and editors, like VSCode
.vscode/
.idea/

# Build output directory, if you're compiling your project, should be ignored
dist/
build/

# Ignore any test coverage directories if you're using a tool like Jest for testing
coverage/
